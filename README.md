1. Install Dependencies
 
 - $> sudo apt-get install npm

2. Spawn database
 
 - Open a terminal
 - $> cd backend
 - $> nmp start

3. Spawn the web server

 - Open a terminal
 - $> cd frontend
 - $> nmp start
